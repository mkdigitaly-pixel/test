---
description: Правила проекта весы-архипов.рф (Невские весы, Новосибирск)
globs: vesy-arhipov/**
alwaysApply: false
---

# весы-архипов.рф

- Проект: папка `vesy-arhipov/`.
- Сайт: https://xn----ctbbkbt8cfbe.xn--p1ai/ (IDN: весы-архипов.рф), ООО «Архипов» / Невские весы Новосибирск.
- Выгрузка как `raytechi_prochee.tsv`.
- **Актуальные весы:** `python parse_all.py` → `data/vse_vesy.tsv` (+ xlsx) — каталог `/vesi/`.
- **Архивные модели** отдельно: `python parse_archive.py` → `data/arkhivnye_modeli.tsv` (+ xlsx).
  Группы: Лабораторные → Торговые → Товарные → Крановые → Автомобильные.
  Источник: `весы-архипов.рф (архив)`.
- Лабораторные (актуальные) отдельно: `data/laboratornye_vesy.tsv`.
- Источник актуального каталога: `весы-архипов.рф`.
- Цены на сайте часто нет — поле пустое, не выдумывать.
- Модели из HTML-таблиц; опции/аксессуары (GP-04, EJ-02, AD-…) не включать.
- Порядок групп — как в меню `/vesi.html`.
- ПО `/vesi/progi.html` не входит в основную выгрузку; архив — только через `parse_archive.py`.
